import { Field } from '../types/form';
export default function attachEventListeners({ field: { ref }, handleChange, isRadioOrCheckbox, }: {
    field: Field;
    isRadioOrCheckbox: boolean;
    handleChange?: EventListenerOrEventListenerObject;
}): void;
